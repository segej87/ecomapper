<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kora.android">

    <!-- Permissions for internet access, location services, camera use, and writing to storage -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:name="com.kora.android.KoraApplication"
        android:allowBackup="true"
        android:icon="@mipmap/kora_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        android:debuggable="true"
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="org.koramap.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity
            android:name="com.kora.android.StartScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.kora.android.NewMeas" />
        <activity android:name="com.kora.android.LoginActivity" />
        <activity android:name="com.kora.android.NewPhoto" />
        <activity android:name="com.kora.android.NewNote" />
        <activity android:name="com.kora.android.Notebook" />
        <activity android:name="com.kora.android.ListPickerActivity" />
        <activity android:name="com.kora.android.CameraActivity" />
    </application>

</manifest>